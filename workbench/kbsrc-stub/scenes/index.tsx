// stub：skill 标准工程的场景总表（拆解契约核心）——SCENES: {镜头 id → 场景组件}，SCENE_PARAMS: {镜头 id → 语境参数表}
import type React from "react";
export const SCENES: Record<string, React.FC<Record<string, unknown>>> = {};
export const SCENE_PARAMS: Record<string, readonly unknown[]> = {};
