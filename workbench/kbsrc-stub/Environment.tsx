// stub：环境层。promo 工程 = 呼吸暗角 + 三色扫（Environment / ShapeWipes）；skill 标准工程 = 幕底画布（Environment，画在镜头之下）+ 幕级覆盖（Overlays，画在镜头之上）
import type React from "react";
export const Environment: React.FC = () => null;
export const ShapeWipes: React.FC = () => null;
export const Overlays: React.FC = () => null;
